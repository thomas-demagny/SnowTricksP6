{% extends 'base.html.twig' %}

{% block title %}Modification{% endblock %}

{% block body %}
    <main class="limiter">
        <div class="d-flex justify-content-lg-between">
            <h1 class="justify-content-start">Modification de l'utilisateur</h1>
            <div class="justify-content-end">
                {{ include('user/_delete_form.html.twig') }}</div>
        </div>
        <div class="d-flex justify-content-lg-end">

        <a href="{{ path('change_password', {'id': user.id}) }}" class="btn-sm btn-secondary mt-3">Modifier le mot de passe</a>
        </div>
        {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <div>
            <a href="{{ path('user_index') }}" class="btn-sm btn-dark"><i class="fas fa-angle-double-left mt-3"></i>retour</a>
        </div>
    </main>
{% endblock %}