{% extends 'base.html.twig' %}

{% block title %}Modification de figure{% endblock %}

{% block body %}
    <main class="limiter container">
        <div class="d-flex justify-content-lg-between">
            <h1 class="justify-content-start mr-4">Modification de figure</h1>
            <div class="justify-content-end">
                {{ include('trick/_delete_form.html.twig') }}</div>
        </div>
        {{ include('trick/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('home') }}"> Retour</a>
    </main>
{% endblock %}